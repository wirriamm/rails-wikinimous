<div class="d-flex">
  <h2><%= @article.title %></h2>
  <!-- <i class="fas fa-edit px-2"></i>
  <i class="fas fa-trash-alt px-2"></i> -->
  <div class="mx-2"><%= link_to 'Edit', edit_article_path %></div>
  <div class="mx-2">
    <%= link_to 'Delete',
        article_path(@article),
        method: :delete,
        data: { confirm: 'Confirm delete?' } %>
  </div>
</div>
<p><%= @article.content %></p>
